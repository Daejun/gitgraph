▾ @@ -220,6 +220,7 @@ static int f2fs_write_page(struct 
    220       if (!page)
    221 -         return -ENOMEM;
⚠   221 +         goto out_unlock;
    222 + 
    223       spin_lock(&sbi->lock);
    224   out_unlock:
    225       spin_unlock(&sbi->lock);
    226       return 0;

